# Change Log

## [2.0.5]

### Fixed

- 更新说明更正：2.0.4 里「归档之后可以在侧栏底部还原」那条写早了——那个入口只在更新的 dsh 预览版上出现；装支持区间内的 dsh 时，还原在 0.1.6 的设置页那一节，而 0.1.5 线上官方没有取消归档（归档之后不可逆）。

## [2.0.4]

### 上游兼容性

- dsh 版本支持：npm 上 `latest` 与 `next` 指向的 0.1.5 线已实测通过——按官方文档一路装下来（含一键安装命令）装到的就是可用版本，不用再自己挑版本。
- dsh 预览版兼容：装更新的 dsh 预览版时，侧栏与对话面板不再整页空白，打开就能看到工作区、会话与对话内容（那些版本仍在适配中，不在支持区间内）。

### Fixed

- 面板整页白屏：dsh 服务重启之后（含升级后的自动重启），侧栏与对话面板不再整片白掉，重新打开就是正常内容。
- 归档会话：归档之后可以还原了——侧栏底部的「已归档」一节列出被归档的会话，点「取消归档」它就回到原来的列表里。
- 插件装多了整页打不开：profile 里装了第三方插件之后，侧栏与对话面板不再整页起不来（以前只要那个插件的打包写法跟官方不一样就会连累整批插件都装不上）。
- 页面加载自愈：某一批插件真出问题时，页面不再把一大批插件从自己的清单里摘掉、越摘越空，而是照实给一行说明；之前自动摘掉的记录在页面恢复正常后也会清掉。

## [2.0.3]

### Fixed

- 设置面板：面板被关掉之后再点齿轮，不再一声不响——会把设置页重新开出来；万一还是开不出来，也会给一行说明，而不是什么都不发生。

## [2.0.2]

### Fixed

- 服务停摆时不再刷屏：dsh 一度连不上时，页面不再对着同一个接口每秒重试几百次，日志也不会被同一条错误填满（别的问题线索留得住）；服务恢复后照常。
- 页面加载自愈：某个插件在新版 dsh 上起不来、整页被错误提示挡住时，页面会自己把它摘掉后重载一次（只试一次）；还是不行就给一行说明与「重新加载」入口，不再一直停在错误页上。

### 上游兼容性

- dsh 新版兼容：升级到新版 dsh 后，VS Code 侧栏不再显示装不起来，工作区与会话照常列出来。
- dsh 新版兼容：升级到新版 dsh 后，打开对话面板时盖着的那层「正在打开会话…」不再白等满 5 秒才揭开，面板标题与右侧栏也照常跟着当前会话走。

## [2.0.1]

### Fixed

- 对话面板：面板被关掉之后再点会话、或者新建对话，不再一声不响——会在原位把面板开出来；万一还是开不出来，也会给一行说明，而不是什么都不发生。

## [2.0.0]

### Added

- 行内改名：点一下已经在看的那条会话，标题就地变成输入框（原来的名字已选中），回车保存，Esc 或点到别处放弃；点别的会话行仍是打开它。
- 会话行菜单：行尾「⋯」与右键共用同一份菜单，含「选择多个 / 在新标签页打开 / 重命名 / 置顶 / 标为未读 / 移到分组… / 分叉会话 / 复制引用 / 移入回收站 / 归档会话」，菜单顶部写明是哪条会话。
- 工作区行右键菜单：复制文件夹引用、把工作区归到分组、归档该工作区全部会话、在新窗口打开文件夹、复制路径、重命名工作区、从列表移除。
- 工作区行悬停四枚按钮：新建会话、打开终端、在 VS Code 打开（当前工作区不显示）、从列表移除。
- 当前工作区标识：VS Code 里打开的那个文件夹对应的工作区排到列表最前，行尾显示蓝色「vscode」标签，文件夹图标同时高亮；打开或切换会话不会改变工作区顺序。
- 会话多开：会话行右键（或行尾「⋯」菜单）选「在新标签页打开」，该会话单独开一个标签页，方便两个会话并排对照；点会话行仍是原处就地切换。
- 侧栏状态提示：没装 dsh、服务没在跑或正在启动、侧栏装不起来，三种情况各自给出说明和对应按钮，不再一律报「侧栏加载失败」。
- 安装引导页：安装方式独立成一个标签页，按系统给出可复制的一键命令，官方安装文档作为其中一条入口保留。
- 回收站：会话行菜单选「移入回收站」就把暂时不用的会话从列表挪走，随时能还原；底部入口显示条数，点开是从底部滑出的抽屉，收进去的会话按原工作区分块列出、块可折叠。
- 标签组：会话行菜单的「移到分组…」里能选组归组、也能移出分组；组能改名、换色、删除，拖组头调顺序；把会话拖到组上就归组、拖到组外就移出，组头可折叠并显示组内待处理条数，组内会话可置顶到该组最前。
- 侧栏多选：可以一次勾选多条会话批量处理，工作区分组头点一下把这一组能选的会话全勾上（组里有置顶会话时会说明为什么选不满），搜索结果里的会话同样能勾。
- 批量移入回收站与批量归档：勾选后可一次把多条会话移入回收站（立刻生效并提示条数），或一次归档（先弹确认并说明有几条会被跳过）；没成功的会话留在勾选里并写明条数，方便直接重试。
- 侧栏空状态：还没有添加工作区、选中分组里没有工作区、列表还在加载，三种情况各自给出说明（分组空态还带一个「管理分组…」入口），不再是一块空白。
- 侧栏会话行显示活动定时任务：有活动定时任务的会话在标题后显示一枚闹钟标记，搜索结果行同样显示。
- 管理分组：点某个分组的名字就能挑成员——列出全部工作区，逐条点一下即入组或出组，可以按工作区名搜索，也能对当前筛出的那些工作区一次全选或清空。
- 侧栏搜索结果高亮：标题、工作区名与命中片段里的搜索词用蓝色加粗标出来，同一个词在一行里出现几处就标几处，一眼看出这条为什么被搜到；片段只标命中词，长文本仍按原来的省略号截断。
- 管理分组：分组列表里每个分组前面多了一个拖拽把手，抓住它上下拖就能换分组的先后顺序，松手即生效；悬停时会显示插入位置，拖回原位不会改写任何东西。
- 回收站里可以点右键开菜单：在抽屉里的会话行上点右键，直接出「还原」与「永久归档」两项，和行尾那两枚按钮是同一份动作。
- 回收站的行显示会话状态：正在运行、等着你回复、还没读过的会话在抽屉里同样带状态点（未读的那条标题加粗），置顶的会话前面有图钉——和侧栏列表里的显示一致。
- dsh 更新检查：状态栏动作面板与悬停气泡里都能查 npm 上的新版本，查到后点「升级」即可在终端里装好（装完重启 dsh 服务生效）；命令面板另配「检查 dsh 更新」「升级 dsh」两条命令。
- 局域网访问：开启后同一局域网内的设备能用带 token 的链接打开 dsh 网页；状态栏气泡标明当前是否局域网可达，关着时可一键重启切换（dsh 出于安全不允许直接开放到网络，dsh One 用本机转发的方式实现，dsh 本身仍只监听本机）。
- 复制访问链接：状态栏动作面板与命令面板可一键复制带 token 的本机/局域网访问链接。
- 侧栏 ＋ 菜单添加工作区之后就接着开新会话：选完已有文件夹或创建好新目录，该工作区的新会话页会直接打开，不用自己再点一次新建。
- 侧栏添加工作区后的提示：新加的工作区被当前分组过滤挡住时，顶部给一条说明并带「全部工作区」按钮，点一下就能看到它。

### Changed

- **前端整线换成 dsh 网页版同一套 cordis 插件形态**：对话区跑的就是官方那批组件，**网页版能用的插件在这里基本都能用**（卡片、工具、面板照见），旧的自研聊天区整块下线；例外只有往官方外框与官方侧栏里塞东西的那类——这两块归 VS Code，侧栏是我们自研的会话树。
- 标签页标题与图标：对话、设置、安装引导三个标签页的图标恢复成 dsh 的品牌图标（早先那一版就是这样，中间一度退回成了 VS Code 的默认图标）；标题统一成「dsh · 名字」的样子——还没拿到会话名时先显示工作区名，不再出现「装配」这类内部说法和一串会话编号。
- 回收站抽屉头补齐三样：左边是「返回」（原来那枚关闭按钮的位置），标题右侧多了「清空回收站」与「恢复全部」两枚，回收站空着时它们是灰的；条数跟在标题后面。
- 标签组恢复预置的 Todo / Doing / Done 三组：任何工作区里都能把会话移进这三组，它们删不掉、也不能改名（名字跟着界面语言走），空着的时候不占位（没有会话就不占那一行，移进会话才出来）；升级后原先归在这三组里的会话重新显示在组里。
- 标签组：组内会话不再多缩一层，同组改用一条与标签同色的竖线表示。
- 工作区行不再有「⋯」按钮：重命名工作区、删除工作区与分组归属都移到右键菜单里，悬停位置留给新增的四枚按钮。
- 归档会话改成删除语义：选中「归档会话」先弹确认（写明归档后不能在这里恢复），确认后会话从列表消失、不进回收站；「移入回收站」与「归档会话」因此分成两个菜单项。
- 搜索结果显示时提示「仅显示前若干条结果」，并按上限条数提示缩小搜索范围。
- 侧栏搜索回到网页版的做法：平时是顶栏上的一枚放大镜，点开才展开成输入框，按 Esc 或点「清除」收起并清空；有输入时保持展开，点别处不会把正在看的结果收走。
- 侧栏动作失败不再无声：归档、分叉会话、在新标签页打开失败时都会飘出一行说明。
- 回收站入口：主区图标从归档箱换成垃圾桶；点一下展开抽屉、再点一下收起（此前点了只会展开、不会收起）。
- 侧栏标题文字不再压小：工作区名、会话标题、行内改名输入框、回收站抽屉标题与底部入口行的字号回到网页版侧栏那样（此前比网页版小一号）。
- 侧栏的行改成网页版那样大：工作区行与会话行的高度、圆角与行内留白用网页版侧栏的尺寸（工作区行比会话行略高，此前两边都是压小的一行高），行里的图标与状态点照旧居中；右键菜单与其它控件保持原来的紧凑尺寸。
- 侧栏去掉顶栏的「视图选项」菜单：分组固定为按工作区、顺序与 dsh 网页版一致；之前选过「单列表」或「最近更新」的会回到这一种形态。
- 工作区行的会话数挪到工作区名字后面：运行中 / 等待交互的计数从行的最右边挪到名字右侧，一眼能看出是哪个工作区在忙；工作区名字太长时先用省略号让位，计数照常显示。
- 工作区名后面的计数补回「未读」一项：除运行中与等待交互，还显示这个工作区里未读的条数（和旧侧栏一样）；三项都没有时整枚角标不显示。
- 工作区行的「vscode」标签比原来小一号：高度与左右留白都收紧，和行内其它小件的尺寸更协调，窄侧栏里也不再压到工作区名字上。
- 侧栏顶部只剩一行：分组过滤胶囊与搜索、折叠/展开全部、添加工作区、设置这些按钮并排在同一行（此前分组过滤条自己占一行）；点搜索时输入框占满整行，胶囊与那几个按钮先收起来，按 Esc 或点「清除」收起后原样回来。侧栏很窄时收起来的是分组名（名字过长会截短），其余按钮尺寸不变。
- 回收站抽屉里的分组标题改成侧栏工作区行那种形态：同样的折叠箭头（位置与工作区行一致），分组名跟着对齐到同一列，行高与圆角也和工作区行一样；点一下收起、再点一下展开的用法不变。
- 回收站里每条会话的行尾直接摆出「还原」与「永久归档」两枚图标按钮，不再把归档藏进行尾的「⋯」菜单；「永久归档」用红色标出来，点了仍先弹确认再执行。
- 拖动会话时被拖的那一行变半透明，一眼能看出正在拖的是哪一条；松手后恢复原样。
- 状态栏点击改为打开动作面板：面板按当前状态列出打开浏览器、检查更新（或升级）、复制访问链接、重启/停止服务、显示日志等动作，一个动作一行（不再点击就直接跳浏览器）。
- 状态栏悬停气泡的动作改一行一个（原来挤在一行里，会按气泡宽度拦腰折行）。
- 状态栏悬停气泡变窄：说明文字拆成短句、网址与版本各自一行，气泡不再被长句撑到半屏宽。
- 安装引导页改版：安装命令与复制按钮并排一行（点「安装 dsh」可换平台），下面分「终端安装 / 编辑器接入」两段，装完从编辑器一段直接打开 dsh 网页版。
- 侧栏更紧凑：顶栏、分组过滤条、回收站入口行与回收站抽屉一起收紧，贴近 VS Code 侧栏的密度（网页版仍是官方原样）。
- 侧栏留白：顶栏那一行、分组过滤条与列表之间的上下空隙恢复原样，不再糊成一坨（左右仍是 VS Code 侧的紧凑密度）。
- 侧栏里的对话框也收紧了：管理分组、标签组的新建与删除、归档确认、重命名、删除工作区这些弹窗的标题、输入框、按钮、列表行与间距都改成与侧栏同样的紧凑尺寸，侧栏拉窄时也不再挤破或裁字。

### Fixed

- 对话面板：要打开的会话已经不在了（被删掉、或已经收进回收站/归档）时，面板直接显示新对话页，可以立刻开始新一轮，不再停在一片空白。
- 侧栏搜索：按聊天内容搜出的会话以前一条都不显示（只能搜到会话标题与工作区名），现在内容命中的会话照常列出来，并带一小段命中片段。
- 对话标签页：重载窗口或重启 VS Code 之后，之前开着的对话标签页会自己回来，并落回原来那个会话（多开的几个标签页各自回到各自的会话）；此前这些标签页会整个消失，只能重新打开。会话若已被删掉，面板会照常打开并说明一句，不再默默换成别的会话。
- 侧栏与对话面板：插件多的 dsh 上打不开的问题修好了——装过自有插件包之后同样打不开的那一处也一起修好。
- 侧栏工作区：手动收起过的分组（含当前会话所在的那个）重开窗口或刷新列表后不再自己弹开。
- 侧栏顶栏的「折叠 / 展开全部」图标以前比同一排其它图标小一圈，现在和它们一样大。
- 回收站入口行：清空与恢复两枚图标以前紧跟在「回收站」文字后面，现在贴在行的最右边；这一行的图标大小与间距也回到旧侧栏的样子。
- 会话行点击：刚打开编辑器时点一条会话行，现在会真的把右侧对话打开（此前只进入改名、对话区一直不出现）；已经在看的那条会话再点一下仍就地改名。
- 未分组会话那一行的「＋」：以前点了没反应，现在能新建一条不属于任何工作区的会话。
- 回收站抽屉：收起时也有滑出动画，不再一瞬间消失（原来只有展开有动画，两边不对称）。
- 侧栏状态提示：dsh 服务在别处停掉或起来之后，提示跟着更新，不用先切走再切回来。
- 侧栏「选择已有文件夹…」与「归档会话」：这两处此前因为读取官方工作区服务的方式不对而必定失败（前者点了没反应，后者只能看到归档失败），现在都正常工作。
- 侧栏多选的操作条：条上的「未选任何会话 / 已选 N 项」不再被挤成一字一行，窄侧栏里放不下时三枚按钮整行下移，不再互相挤压文字。
- 侧栏多选的操作条：条与分组过滤条、会话列表之间加了分隔线和底色，按钮不再像是浮在会话行上；三枚按钮也改小一号，与这一列的其它控件同高。
- 侧栏多选：进入多选后，工作区名不再被勾选框单独推到右边，它与会话标题之间的缩进关系原样保持。
- 侧栏多选：会话行的勾选框往里缩进一档、与工作区行的文件夹图标对齐（工作区行的框仍在最左），会话标题的位置不变。
- 侧栏菜单：会话行「移到分组…」与工作区行「分组…」展开出来的二级项不再往右缩一截，文字与父项文字对齐（左边仍留着图标那一列，标签组的色块画在那里）。
- 侧栏菜单：「移到分组…」与「分组…」右端的展开箭头以前又小、又紧贴着文字，现在换成网页版侧栏那两枚箭头（收起朝右、展开朝下），并靠到菜单项的右端；标题再长也只是文字先截短，箭头不会被挤掉。
- 侧栏左缘对齐：顶栏的搜索框、分组过滤条上的分组胶囊、列表行里文件夹图标的左缘落在同一条竖线上，不再一个贴到侧栏边、一个往里缩（回收站入口行与抽屉里的行一起跟上）。
- 侧栏会话行：等待提问或审批的会话现在前面显示黄点，此前这类会话显示的是绿点。
- 侧栏顶部那一行的右边留出空白：搜索与工具按钮、展开后的搜索框都不再顶到侧栏右缘，右端与列表行行尾内容（时间、角标）落在同一条竖线上。
- 侧栏顶部那一行：macOS 上不再比其它平台多缩一截——工具图标与展开后的搜索框的右端，和列表行行尾内容（时间、角标）落在同一条竖线上。
- 侧栏搜索结果：正在等审批或回答、正在运行、跑完还没打开的会话，在搜索结果行里也显示对应的状态点（此前搜索结果一行状态标记都不显示）。
- 侧栏状态点：跑完时正开在对话面板里的那条会话不再亮绿点（包括在单独标签页里开着的那条），此前会误报「跑完还没打开」。
- 侧栏：某条会话正开在别处的 dsh 里时，点它会当场说明原因（那条会话被另一个 dsh 占用，先去那边关掉它），不再只在发消息时弹一条看不懂的失败提示。
- 插件里的外链：设置页里「打开官网 ↗」「获取密钥 ↗」这类链接在 VS Code 里点了能打开系统浏览器（此前点了没反应）。
- 侧栏：刚装好、还没有任何历史数据时第一次打开侧栏，树不再是一片空白（此前只有用过一段时间的环境才显示得出来）。
- 侧栏：点过顶栏右侧的按钮之后，整条侧栏的内容（搜索框、分组标签、列表行）不再整体往左挪一小格、贴到侧栏边上（挪过去之后它自己也不会挪回来）。
- 界面更新会立刻生效：升级扩展后重载窗口，看到的就是新界面，不再出现「升了级但界面还是旧的」那种情况（此前要重启 dsh 服务或者等一天缓存过期才看得见）。
- 侧栏 ＋ 菜单添加或创建工作区失败时不再无声：现在会在页面上给出一行说明（此前选完目录没有任何反应，也看不出到底成没成）。
- 对话区里的 HTML 预览卡（模型画的可视化卡片）：现在按内容完整撑开、与网页版 dsh 里的显示一致，此前被压成一条窄框、只露出内容最上面一小条，还得在框里自己滚。
- 插件卡片：卡片里用到的图片、字体不再被挡，卡片里的点按也照常响应，显示与操作和 dsh 网页版一致。
- 对话区断线提示：dsh 重启或连接中断时，对话区下方出现一行说明，重连成功后自己消失（此前断线全程没有任何提示，内容会自己回来、但看不出来刚才断过）。
- 对话面板：打开面板时，目标会话稍晚才出现在列表里的情况不再停在空白页，会继续等到它出现再打开（此前只看一眼列表，那一刻没有它就再也不管了）。
- 白屏：页面内容整个消失时不再只剩一片白，顶上一行说明带原始报错与「重新加载」，点一下页面自己恢复（重启 dsh 服务后偶发的那种白屏不必再自己按刷新）。
- 标签组：新建一个组时，默认选中的颜色优先挑这个工作区还没用过的，不再一上来就和已有的组撞色；六个颜色照旧都能手选。
- 标签组：组里最后一条会话移进回收站时，这个组当场消失，不再等到归档那一步；组已经不在了的会话从回收站还原时落在「未归组」。

### 上游兼容性

- dsh 新版兼容：升级到新版 dsh 后，侧栏「等待交互」的黄点照常显示。
- dsh 新版兼容：升级到新版 dsh 后界面不再整片空白，侧栏里的会话照常列出来，点会话也能正常打开。
- dsh 新版兼容：升级到新版 dsh 后，会话「跑完还没打开」那枚绿点照常显示。

### 内部优化

- 侧栏树的界面裁剪与隐藏口径收敛、自有插件整理成可安装的插件包、占用提示的判断依据更新、新增一条交互自检（把关键控件逐个点一遍）等内部重构，界面看起来与以前一样。

## [1.2.0]

### Added

- 标签组：会话按自定义标签分组成块，整块折叠/展开、重命名、换颜色，会话可跨组拖拽、改名行内保存。
- 标签组按工作分开：每个工作区维护各自独立的标签组，切换工作区互不串扰。
- 标签组内新建会话：标签组组头右键「在此分组新建会话」，新会话自动归入该组。
- 批量派生自动归组：一次派生的多个会话自动进入同一个标签组。
- 行内码交互：消息里的代码路径可点击打开文件，代码片段悬停高亮、右键即可复制。
- 输入草稿持久化：输入框里没发出去的文本和附件，重启后仍在。
- 工具输出图片：模型返回的本地图片路径直接在消息里渲染缩略图，图片缺失或超限时显示占位。
- 断连提示：dsh 服务断开时聊天区显示重连状态（连接中 / 失败 / 已恢复），并提供「立即重连」。

### Changed

- 会话状态统一存到本机 dsh 数据目录：分组、标签组、置顶、未读、回收站等状态跨窗口共享、重启不丢，并以文件为准（不再随各 VSCode 窗口各自存）。
- 打开会话只高亮不跳序，并移除会话列表的排序开关（点开会话不再把列表重新排序）。
- 活跃会话排在标签组上方；标签组内部的活跃会话留在组内并置顶。
- 会话行状态标记移到行尾时间位并与顶层行右缘对齐：运行中 / 未读 / 置顶改为互斥显示，置顶图钉移到标题前。
- 分组栏改为胶囊标签，新建分组收进下拉菜单（不再占满整行）。
- 模型推理等级默认档对齐官方（Default 档，切换模型时重置）。

### Fixed

- model 选择 pill 打开会话首页即显示真实模型名（不再先闪「选择模型」）。
- 插话快捷键提示按平台显示：Windows/Linux 显示 Ctrl+Enter，macOS 显示 ⌘Enter。
- 双击清空 + 反悔：composer 双击 Ctrl+C / ESC 清空输入（带确认提示），Ctrl+Z / 反悔恢复。
- 长文本输入：composer 长文本滚动窗口跟随光标、文本行宽对齐，长文本里的 @ 引用不再逐行漂移。
- 回收站：会话按进入回收站的顺序排列（最新在最上），并按平铺方式展示（不再保留标签组嵌套）。
- 回收站操作：抽屉提手可点收起，回收站入口行常驻「清空 / 恢复全部」快捷操作，清空图标改为错误红提示危险。
- 标签组自动清理：会话全部移入回收站后空的标签组自动删除；归档完成时也已无活跃成员的组一并清理。
- 排队消息：切换会话后不再丢失，排队中消息原样保留。
- 排队卡片预览：@ 引用渲染成可读标签（不再显示超长原始链接占满两行）。
- 消息图片缩略图：增量刷新后不再停留在旧图。
- 任务进度圆环：流式更新期间任务真更新时从当前相位接续，不再从 0° 重新启动。
- 展开块：内容底部新增「收起」按钮，不必滚回顶部才能折叠。
- 回合轨道栏：窄面板（≤760px）自动隐藏，刻度线不再压住文字。
- 会话列表滚动位置：折叠 / 展开分组时不跳回顶部，滚动位置保持。
- 多选操作：点回收站 / 归档即退出多选，操作条短文案不再折行。
- 聊天内容列限宽居中（748px），代码块复制按钮不再压到消息区右缘。

### 上游兼容性

- dsh 0.1.3 兼容：升级后聊天流式输出、斜杠命令照常运行。
- 会话头部预设标签：预设名称与 dsh 状态一致（对齐新版本 dsh 的字段）。

### 内部优化

- 前端组件抽取共享基座、消息行增量重建、输入框保活与 IME 组合兜底、组管理弹层焦点保活、dsh 版本适配层等重构与兼容性适配。

## [1.1.0]

### Added

- 外部启动的 dsh 实例完全接管（external-dsh-manage-012）：状态栏 tooltip 标记 running+external 实例并给出管理入口；四个管理命令（粘贴 token 连接 / 复制 token 模板 / 停止 / 重启）带确认弹窗；停止只向单 pid 发信号（POSIX SIGTERM 优雅关闭 + 超时 SIGKILL，Windows `taskkill /T /F`）；token 连接走 `GET /?token=` 换票校验，成功写入共享记录 `source: external / owned: false`（kill 权不归任何窗口）；防护默认动作改为「认证 dsh 无可用 token → 报错不另起」，不再 fallback 换端口 spawn 双实例；非 dsh 的 foreign 占用保留原行为。

- 会话打开失败提示条（session-open-failure-no-hint）：打开会话失败（日志损坏 / 会话不存在等）不再被前台静默吞掉——webview 整页显示「Failed to open session」标题 + 建议 + 等宽小字原因行（含未就绪 `loading` 占位隔离，避免闪空态 hero）；再点一次该会话 = 重建 controller 重试；失败清除时立即推帧恢复。

- 视口尺寸变化统一补偿（composer-multiline-input-jitter）：输入框跨行增高（和 todo/queue dock 开合、窗口 resize 等一切改变消息区 clientHeight 的扰动）不再引起聊天区「顶出-拽回」上下跳动——`.messages` 挂 ResizeObserver，跟随态在 layout 后 paint 前同帧钉回底部；全部 8 处 `scrollTop` 写点收口为 `writeMessagesScrollTop` 原语（写后读回 clamp 落点 + 登记程序 pin，结构上杜绝回声锁漏登记）。

- 沙盒验收驱动防挂死（verify-driver-hang-investigation）：根因是 Playwright 已知缺陷（对「挂起导航、无执行上下文」的 iframe 调 `evaluate()`/`locator.count()` 永不返回，try/catch 与 `{timeout}` 选项均无效）。修复：帧扫描循环跳过空 URL 帧；所有无超时调用点接 `bounded()` 竞速看门狗（默认 10s，转该项 fail + 帧快照诊断）；每项 5min 硬上限兜底（转储 `page.frames()` 各帧 URL）；首项前冒烟预热 + 单项目 fail 自动重试一轮。

- 状态栏 tooltip 显示 dsh 版本（statusbar-dsh-version-tooltip）：running 态标题下加 `dsh v{version}`——版本来自定位 dsh 时执行的 `--version`，经共享记录持久化（reload/多窗口收养后仍知道）。

- 状态栏 tooltip：adopted / external 实例也显示 dsh 版本（statusbar-adopted-version）：旧实现一刀切不显示（怕误导）——现在 shared 记录优先（spawn 时已存 version，另一窗口 adopt 直接展示）；无记录时从实例命令行解析真实入口（`@deepseek-ai/dsh/lib/bin.js` / 官方 `dist/dsh.js` / npm 全局 shim / Windows `dsh.cmd` 等形态）执行 `--version` 探询，不依赖扩展 PATH（多安装不误导）；探测失败才缺省不显示。

- 另一窗口 spawn 的实例（adopted）提供确认式停止/重启（adopted-manageable）：此前 adopted 只有复用入口（kill 权归 owner 窗口），单用户多窗口（主窗口 + isolated dev 窗口）场景下第二窗口无法调度实例——现 tooltip 与外部实例同款提供「停止/重启外部实例」入口，确认弹窗文案统一覆盖「终端或另一窗口启动」两种来源；停止仍走 pid→命令行 dsh 特征→单 pid SIGTERM 安全校验。

### Fixed

- 流式输出中 commit 悬浮卡闪关闪开（commit-card-jumps-during-streaming）：消息行每帧重建摘除旧 chip 导致详情卡断开重开——行重建后按同身份重锚已打开弹层（commit chip 按 sha 限同 flow 行；token 用量药丸同机制），卡片被摘除时鼠标判定不排延迟关闭。

- i18n 合入门禁误报（check-i18n.sh）：`return /regex/` 前是字母（如 `n`）不在操作符表里被当除法解析，状态机错位后其后的中文注释剥离失败、被误判漏翻——补关键字回溯识别（`return|typeof|case|…` 后紧跟 `/` 视为正则），不弱化硬编码中文检查。

- Windows 自愈（recover-token-from-log / recover-token-no-record）：扩展 spawn 的 dsh 冷启动慢（Windows 60s+），用户在 waitReady 完成前 reload/关闭窗口 → token 补写没执行、记录停在无 token 版（且会被防护流程清掉）→ 下次启动身份无法确认、进防护死循环（实例活着却连不上）。现在「有记录无 token」与「无记录 + 认证实例」两条路径都会从 dsh-web.log 重解析就绪行 token 换票，成功即补写记录 re-own 自愈；换票失败/无就绪行才落空走原清记录+防护（不退化）。

### Changed

- 沙盒验收基建：`scripts/ui-visual.sh` 支持场景 `interactSteps` 分步交互截图（harness `window.__interactStepDone` / `__interactStepAdvance` 信号严格对齐交互状态，非固定延时）。

## [1.0.0]

### Fixed

- 插话（⌘/Ctrl+Enter，等待落地的 steering 消息）气泡只渲染纯文本：附件显示成「[图片 ×1] [文件 ×1]」计数前缀、会话引用显示原始 `@[标题](dsh-session:…)` markdown。现在与正式用户消息共用同一套渲染——host 从 queue 帧内容提取图片（attachmentId）与文件（`<attachment>` 行）随快照下发，webview 剥附件行、把 canonical mention 展开成可读 `@label`，气泡渲染图片缩略图、文件 chip、会话引用 chip 与「引用会话 · …」摘要行；行结构不变（气泡左侧处理中圆圈）。

- 窗口 reload / VSCode 重启后已打开的 tab 全部丢失（会话 chat tab 与 `DSH One: 在编辑器标签页中打开` 的 dsh web tab）：注册了 WebviewPanelSerializer（`dshOne.chatPanel` + `dshOne.tab`）。会话 tab 以创建后不变的 tabId 为恢复凭据——webview 内容经 `acquireVsCodeApi().setState` 保存，真实会话经 workspaceState 的 tabId → sessionId 映射查询（tab 打开/关闭/会话替换时增量维护），reload 后按 VSCode 还原的面板（位置/active 不变）重建：服务运行中直接附着 controller，未运行先显示空态、服务恢复后走既有 lastActive/pendingRestore 链补附着；dsh web 面板重新绑定服务状态并刷新内容。用户手动关闭的 tab 不恢复，会话数据本身不受影响（dsh 服务与 VSCode 生命周期解绑，此前已如此）。

- 切换会话时先闪一帧空会话 hero（「服务未就绪，暂时无法发送」的居中排版）再跳成消息流：根因是 session.history 基线翻页期间 getState 已带 sessionId 但消息为空、canSend 为 false，命中了空会话 hero 分支。ChatState 新增 `loading`（历史基线未就绪时为 true），加载期间只显示「加载会话…」居中占位，hero 与消息流都等基线落地再渲染。

- 旧会话的聊天头部不显示 preset 标签：原实现靠扫会话日志里的 agent-preset/selected 事件，创建时指定 preset 的会话没有这条事件、旧会话也可能翻不到。改为对齐官方 AgentPresetLabel 的渠道——`composeHeader()` 从 session.list 基线取附着会话的 agentPreset id（官方 sessionSummarySchema 字段，host 创建时即定、实测 77 个会话全部有值），经 roster（agentPreset.list）映射成显示名：user preset 显示 roster 的 name 而非裸 id，roster 未就绪或未知 id 回退已知 system id 中文名/原样 id；roster 拉取不再局限于空会话，store 刷新时标签随之更新。后台任务 chip 的 mux 渠道经核对即官方 web 客户端同款（dsh-client-connection 的 openMux → /api/events.mux 的 session/jobs 帧），未改动。

- 会话行首三种标记（运行中像素环/未读蓝点/置顶图钉）位置不对齐、图钉偏小：状态槽加宽到官方 dsh web 的 16px，三种标记统一收进槽内同一位置居中（优先级 运行中 > 未读 > 置顶），图钉从 11px 放大到 13px；组合状态（置顶 + 运行中/未读）时被挤出槽位的图钉退到标题前。

- 会话行首的「运行中」标记与官方不符（原来是绿色小圆点、空闲会话也有灰点）：对齐 dsh web 官方 StateDot——运行中改为 8 格像素环追逐动画（deepseek 蓝，1s 错相旋转），空闲会话行首留空不再画点。

- turn 失败（如模型上下文超限返回 401）时界面只显示一条空 assistant 消息、看起来像没回复：现在按 dsh web 官方样式显示「本轮运行失败」错误行（红点 + 错误 message + 可选 code 角标），错误来自 host `turn/end` 的 error reason。

- 会话列表的相对时间（"N 分钟前"）长时间不刷新：新增 60s 本地 tick，用缓存基线纯重建模型（不发 RPC）更新时间文案。dsh web 官方同样不轮询（渲染时取一次当前时间），此处是超出官方行为的体验修正。

- 新会话被 dsh 自动命名后，会话列表里的标题不更新：自动命名经会话内的 title 投影到达，host 事件流没有对应事件，面板收不到刷新信号；现在附着会话的标题投影变化时会主动重拉一次列表基线。

- 流式输出期间用户展开的折叠块（思考过程、工具输出、注入上下文、问题详情）不再被快照重建冲掉：`<details>` 的展开状态按消息/块位置持久记录，重建后恢复；切换会话时清空。

- 流式输出期间点击权限/模型等弹层会被快照重建冲掉：弹层现在跟随锚点存活（锚点被重建才关闭，布局移动时重新定位），锚定在输入区内的弹层会钉住 composer 不被重建。

### Changed

- 空会话 hero 的 preset（Agent 模式）与权限模式切换改为**懒更新**（与 workspace 懒切换同模式）：点选只记录 pending 并就地更新 chip/pill 显示，**不发 RPC、不执行 /permission 命令**——真正 `setAgentPreset` / `/permission` 在发送时随消息一起落地。此前切换 preset 会触发 hero 整页重建（鲸鱼动画重播、chip 是 popover 锚点被换掉）；切换权限模式的 `/permission` 命令会直接写进消息流，把空态 hero 变成「输入过问题」的消息流 tab。修法：`agentPreset`/`permissions` 移出 composer 签名、hero 保活分支就地 patch（对齐 workspaceLabel 的既有做法）；`ChatTabHost` 增加 `pendingPresetId`/`pendingPermission`，由 `composeHeader` 覆盖显示、send handler 统一落地。

- 聊天输入区的发送按钮对齐官方 dsh web（hero 空态与普通消息流一致）：文字「发送」按钮改为 34×34 圆形图标按钮（品牌蓝 `deepseek-400/500` 底、白色 16px 上箭头图标、无文字、disabled 半透明）；运行中同一按钮变停止方块图标（官方 primaryStops 交互），点击即停止——原来并排的独立「停止」文字按钮移除；排队发送仍走 Enter（⌘/Ctrl+Enter 插话）。图标取自官方 InputBar 主按钮内联 path（`IconSendOutline16` 发送箭头、`<rect rx=3>` 停止方块）。

- 「发送到当前会话」右键菜单项改为 `DSH One: 发送到当前会话`（标题自带 DSH One 标识，右键菜单不显示 category，不加前缀看不出是谁的菜单）：编辑器/资源管理器菜单里的位置从最顶的 navigation 组移到中间独立分组 `2_dshOne`（与相邻分组自动以分割线隔开）；添加成功后的右下角提示移除（composer 里出现的附件 chip 本身就是反馈，不再弹 toast）。

- 会话面板的 workspace 标识与 dsh web 融合：当前 VSCode 打开的 workspace 行尾标签从「当前」改为「vscode」（语义不变，只是标明"这个文件夹开在 VSCode 里"）；附着会话所在 workspace 的文件夹图标染 deepseek 蓝（对齐 dsh web 官方标识），折叠组同样生效，随附着会话切换实时跟随（复用 syncSessionHighlight 的免重建通道，凭组元素上的 workspaceId 回查快照模型）。

- 空会话（还没有任何消息）的聊天区改为官方 dsh web 空态的居中排版并做本地化定制：居中 DSH One 像素鲸鱼 logo（品牌蓝 #2563EB，约 64px，像素图形取自 assets/icon.svg，轻量游动动画；不用官方 dsh 的鲸鱼标）；无「探索未至之境」标题与「预览版」徽章（用户要求去掉）。其下一行 chip——workspace 名（文件夹图标 + 名称 + 可点开的选择器，来自 workspace.list 基线）和 preset 选择 chip（从 composer 底部挪入，交互不变，下拉向下展开），再下是居中的大圆角 composer 卡片（max-width 780px 自适应收缩、22px 圆角、浮层底色、柔和双层阴影，placeholder 对齐官方「描述你想要构建的内容」，输入字号 16px/24px）。一旦有了消息或 turn 进行中即恢复常规的消息流 + 底部 composer 布局；原「会话还没有消息」提示随之移除（被 hero 取代）。

- 聊天头部样式逐项对齐官方 dsh web 会话头部：标题从 13px/600 改为 14px/20px/500（官方 crumbCurrent），头部 padding 加大（12px 12px 8px）、元素间距 8→10px；「N 个子代理」「N 个后台任务」chip 从徽章底色改为透明底小字（12px，descriptionForeground），文字版「⌄」换成官方 IconChevronDownOutline14 矢量图标，hover 只提亮文字不再整片变亮；只读 preset 标签改为独立的浅底胶囊（22px 高、圆角 6px、最大宽 160px 截断），前置官方 IconAgentPresetOutline16 三环图标（14px、70% 不透明度）。

- 左栏顶部「会话 | 任务」tab 与任务看板界面移除：后台任务的入口收敛到聊天头部的「N 个后台任务」chip（对齐官方 dsh web 形态），看板专属的「显示全部」勾选、详情面板、ActivitySnapshot 推送链路一并删除；jobsStore 的 mux 数据链路保留，改供头部 chip 使用。

- Sessions 树视图合并进 Chat 面板（原生 tree view 移除）：宽屏（≥720px）为左右两栏（左会话列表 260px、右聊天），窄屏改上下布局（会话列表在上、限高 40% 自滚动）。搜索框、排序切换、刷新、新建 workspace/会话、重命名/归档、打开文件夹等操作全部移至面板内；服务未运行/未安装 dsh 的空态与启动、安装引导按钮也随之迁入面板。

### Added

- 编辑器与资源管理器右键文件新增「发送到当前会话」（`DSH One: 发送到当前会话`）：把当前文件作为附件暂存到当前 Chat 面板活跃会话的 composer，与点「添加附件」等价——图片走图片附件（48px 缩略图 + 会话图片限额校验），其他文件以路径 chip 暂存，发送时以 `<attachment>路径</attachment>` 拼进 prompt 让 agent 自己读。没有附着会话时自动附着当前 workspace 最新的会话，一个都没有则新建；Chat 面板没打开过也不丢（附件先落 host 队列，视图 resolve 后再投）。右键目标优先取菜单上下文（explorer 的 Uri / editor 的 resourceUri），编辑器内右键兜底用当前活动编辑器。

- 会话面板的 workspace 行 hover 操作新增「在终端中打开」图标（终端样式，+ 号旁）：点击后以该 workspace 文件夹为 cwd 新建 VS Code 终端并显示，终端名为文件夹名；当前/非当前 workspace 均可用。

- 聊天头部新增「N 个后台任务运行中」chip（对齐官方 dsh-client-ui-jobs 的 JobListAction）：附着会话有后台 job 时出现（有运行中的显示运行中数并带像素环，全部结束显示「N 个后台任务」），点击弹下拉——每行状态点（运行中像素环/已完成绿/已取消琥珀/已失败红）+ kind 徽标（bash 等）+ 命令摘要 + 状态文案（host detail 优先，如 "exit code: 0"）+ 耗时（23秒 / 4分58秒 / 1小时2分，运行中的打开下拉后 1s 跳动），已结束行淡化；行序为运行中在前（开始时间升序）、已结束在后（完成时间降序）。数据复用 jobsStore 的 mux 全局 session/jobs 帧，实时刷新。

- composer 待发送附件列表对齐官方 AttachmentRail：图片附件从文字 chip 改为 48px 圆角缩略图（object-fit: cover，直接用内存里的 data: URL 渲染，CSP 已允许 img-src data:，未引入 objectURL），点击缩略图放大预览，hover 右上角出 × 移除（触屏常显，reduced-motion 去过渡）；加载失败回退为原文件名 chip。文件附件 chip 保留真实文件名并加文档小图标；多个附件横排、超出换行。

- 会话菜单（⋯/右键）新增「标为未读 / 标为已读」：未读是纯客户端状态（官方 dsh 无未读概念与 API），与置顶一样持久化在 workspaceState；未读会话行首显示蓝色圆点（运行中时被像素环占用，仍有标题加粗兜底）且标题加粗，打开（附着）会话自动清未读。官方同样没有自动未读逻辑，故只做手动标记。

- 聊天头部信息区对齐官方 dsh web：标题后新增「N 个子代理」chip（附着会话正在运行的 continuable 子代理，点击弹下拉——每行标题 + 相对时间/token 用量摘要，行点击附着子会话；无运行中子代理时不显示）和只读 preset 标签（如「标准模式」，来自 session.list 基线的 agentPreset 字段，与空会话 hero 里的选择 chip 互斥）。头部标题过长仍 ellipsis 截断，但 hover 显示完整标题，chips 不再挤压标题（flex:none）。

- 对话排版向 dsh web 官方 / kimi-cli 靠拢：工具调用从卡片容器改为行式排版——状态图标（成功 ✓ 绿、失败 ✕ 红、运行中旋转圈）+ kimi-cli 风格英文动作短语（bash→"Ran a command"、read→"Read <路径>"、write→"Using Write" 等，未知工具回退原名）+ 等宽命令/参数预览行（bash 类带 `$ ` 前缀、截断省略）；工具输出默认只显示前 5 行 + 「… 共 N 行，点击展开」提示（点击展开全部、再次点击收起，展开状态跨流式重建保留）；思考过程保持可折叠并改为灰色小字。
- 「Deep diving...」状态行的扫光改为蓝色系渐变（对齐官方 deepseek 蓝，深/浅主题通用），15 秒后的耗时计时逻辑不变。

- 空会话（还没开始任何 turn）出现 Agent preset 选择 chip（最初在 composer 底部模型 pill 旁，后随空态 hero 排版挪到 hero 标题下的 chip 行）：点击弹下拉，一行一个 preset（名称 + 描述，官方四个 system preset 用中文文案：标准模式/PTC 模式/极简模式/创造模式；user preset 用 roster 自己的 name/description），当前选中打勾。选择走主机 agentPreset.select；会话开跑后 host 即锁定（agent-preset-locked），chip 随之消失。

- 对话框右下角的上下文容量条按余量变色并给出实时预估：以 sessionStats 的轮数估算平均每轮上下文增长（perTurn = usedTokens / turns），换算剩余轮数（向下取整）；剩余 ≥10 轮绿色、<10 轮黄色、<5 轮红色，turns < 1 或无法估计时按充足处理。切到更小窗口的模型导致已用量超限时直接红色（与轮数无关），bar 悬停提示与点击弹出的面板里都有超限说明（建议先切回之前的模型执行 /compact 压缩，再切换模型）；面板在可估计时追加一行预估「≈N/轮，约还可持续 M 轮」。

- 发送后等待模型响应期间（turn 打开、尚无输出）在对话流末尾显示状态行「Deep diving...」（对齐 dsh web 官方 TurnStatus 的渐变闪烁动画），超过 15 秒右侧追加每 1 秒跳动的已耗时（如 1m23s）；turn 结束即消失。
- 初始版本：运行时按需下载（Node + dsh）、dsh web 服务管理（探测/复用/spawn/清理）、侧边栏与编辑器标签页内嵌官方 UI、状态栏、自动更新。
- Sessions 侧边栏支持按标题/会话 ID 搜索过滤，以及排序切换（最近更新优先 / 最早更新优先 / 按标题），排序偏好持久化。
- 对话消息级操作：助手消息下方可复制全文、标记有用/没用（对接主机 messageFeedback）、从已完成轮次创建分叉会话；操作行与 dsh web 端图标风格对齐。
- Sessions 视图：标题栏 + 号菜单可添加已有文件夹、或输入名称在 `~/.dsh/workspaces/` 下创建工作区（自动建目录并注册到 dsh；dsh 全局目录不存在时明确报错）；新建会话的 + 移到每个 workspace 行的内联按钮。
- 未安装 dsh 时，Sessions 欢迎页与 Chat 空态显示安装引导，一键打开官方安装页面（deepseek.com/harness）。
- Sessions 面板：元素对齐 dsh web 自身实现——workspace 分组行点击折叠/展开（文件夹图标与 hover 三角箭头、固定行高），会话行 hover 出「⋯」菜单（重命名/置顶/分叉会话/归档会话，带官方图标），右键弹同一菜单；置顶为本地偏好（持久化在 workspaceState，置顶会话排在分组最前），分叉复用主机 session.fork。会话面板与聊天区改用不同背景色（sideBar / editor）区分。
