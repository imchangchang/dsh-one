.vscode/**
.git/**
.github/**
src/**
test/**
node_modules/**
**/*.map
**/*.ts
*.vsix
tsconfig.json
build.mjs
.gitignore
docs/**
scripts/**
.agents/**
.prototype/**
.dev-host/**
.worktrees/**
AGENTS.md
# 自有插件包（#73）：只用于构建。VS Code 侧运行时吃的是 dist/assembly/plugins/
# 里那份产物，宿主半的代码由 esbuild 打进 dist/extension.js，都不需要这个目录。
packages/**
