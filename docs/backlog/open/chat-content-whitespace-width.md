# 聊天内容左右留空（对齐 dsh web 748px 居中）+ 代码块复制按钮与右侧对话条重叠

## 背景与现象（用户反馈）

1. **内容满宽**（用户此前提出，讨论于 session 55c489e9「给 dsh-one 聊天界面留白优化建议」）：当前 dsh-one 的聊天内容左右填满（消息/代码块横跨全宽），dsh web 是留空的——用户认为留空更好。讨论结论：**认同留空更好**；参考：dsh web 聊天内容列**硬编码 748px 居中**（社区有 `dsh-chat-width` 插件做成可拖拽 748→1040px）。
2. **重叠**（v1.1.0 后报告）：**代码框的复制按钮与右侧对话条重叠**——疑似与满宽布局相关（内容无留白空间、按钮定位冲突）；现象以用户截图为准，本条目先记录。

## 线索（待核实）

- 代码块复制按钮：`code-block-collapse-copy` / `chat-code-copy-feedback-flash`（closed）涉及代码块复制 UI——当时用 hover 按钮定位；满宽布局下与右侧消息 action 行（助手消息复制/点赞等）重叠。
- 右侧对话条：消息 action 行（复制全文/标记有用/分叉，见对话消息级操作，v1.0.0 Added）布局在消息右侧；与代码块 hover 按钮的定位/z-index 冲突可能。
- 关键问题：重叠是**满宽**导致的（按钮贴近边缘）还是**独立 bug**（z-index/锚点）——需要截图与本地复现界定。

## 方案（未定）

候选：
1. **宽度收敛**：聊天内容列限宽居中（如 max-width 748px 对齐官方，或可拖拽）；一个改动可能同时解决"满宽"与"按钮贴边重叠"。
2. 若重叠独立于宽度：修代码块复制按钮定位（absolute 相对代码块、不侵入 action 区）或提高 z-index/留边距。

## 涉及代码位置（待核实）

- `src/ui/chat/webview.ts`：消息行/代码块渲染、hover 复制按钮（code-block-collapse-copy 相关）、右侧 action 行容器布局

## 变更记录

- 2026-09-05 用户反馈（代码块复制按钮与右侧对话条重叠 + 追问留白是否记录）→ 核查：留白讨论在 session 55c489e9 有结论但**从未落 backlog**（记录习惯缺口，本次补录）→ 建条目（open/）
